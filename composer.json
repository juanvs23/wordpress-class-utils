{
    "name": "coltman/framework",
    "description": "Coltman Framework — WordPress helpers for CPTs, taxonomies and meta fields",
    "type": "library",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10"
    },
    "autoload-dev": {
        "psr-4": {
            "Coltman\\Tests\\": "tests/"
        }
    },
    "config": {
        "allow-plugins": {}
    }
}
